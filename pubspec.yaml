name: autenticacion_google
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  # --- DEPENDENCIAS DE FIREBASE Y GOOGLE SIGN-IN ---
  firebase_core: ^3.1.0
  firebase_auth: ^5.1.2
  google_sign_in: ^6.2.1
  # --------------------------------------------------
  cloud_firestore: ^5.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
